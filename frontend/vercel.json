{
  "version": 2,
  "builds": [{ "src": "public/**/*", "use": "@vercel/static" }],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/images/(.*)", "dest": "/public/images/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
